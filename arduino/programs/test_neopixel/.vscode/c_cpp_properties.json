{
    "configurations": [
        {
            "name": "Arduino",
            "includePath": [
                "${workspaceFolder}/**",
                "${env:CORESRC}/**",
                "${env:SKETCHBOOK}/libraries/**"
            ],
            "compilerPath": "${env:COREGCC}",
            "intelliSenseMode": "${default}"
        }
    ],
    "version": 4
}
