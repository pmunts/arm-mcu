{
    "configurations": [
        {
            "name": "Arduino",
            "includePath": [
                "${workspaceFolder}/**",
                "${env:CORESRC}/**",
                "${env:ARMSKETCHBOOK}/libraries/**"
            ],
            "compilerPath": "${env:COREGCC}",
            "intelliSenseMode": "${default}"
        }
    ],
    "version": 4
}
